export enum DialogMessage {
    CONNECTION_ERROR,
    INTERNAL_SERVER_ERROR,
    ERROR_SESSION_EXPIRED,
    ERROR_USERNAME_AND_PASSWORD_REQUIRED,
    ERROR_INVALID_AUTHENTICATION,
    ERROR_USER_ALREADY_EXITS,
    ERROR_INVALID_USERNAME,
    ERROR_INVALID_PASSWORD,
    ERROR_INVALID_REPOSITORY,
    REGISTRATION_SUCCESS,
    REMOVE_TRANSACTION,
    ACCOUNT_DEACTIVATED,
    ACCOUNT_ACTIVATED,
    VERIFIED,
}
