import { HttpHeaderConfiguration } from './http-header-configuration';

describe('HttpHeaderConfiguration', () => {
  it('should create an instance', () => {
    expect(new HttpHeaderConfiguration()).toBeTruthy();
  });
});
