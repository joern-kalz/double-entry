import { FormatDateIsoPipe } from './format-date-iso.pipe';

describe('FormatDateIsoPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDateIsoPipe();
    expect(pipe).toBeTruthy();
  });
});
